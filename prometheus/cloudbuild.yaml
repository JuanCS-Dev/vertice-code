steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/vertice-mcp:latest'
      - '-f'
      - 'prometheus/Dockerfile'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/vertice-mcp:latest'
timeout: '1200s'
