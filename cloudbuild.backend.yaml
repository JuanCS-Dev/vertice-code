steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/vertice-ai/cloud-run-source-deploy/vertice-backend:latest'
      - '-f'
      - 'Dockerfile.backend'
      - '.'

images:
  - 'us-central1-docker.pkg.dev/vertice-ai/cloud-run-source-deploy/vertice-backend:latest'

timeout: 1200s
