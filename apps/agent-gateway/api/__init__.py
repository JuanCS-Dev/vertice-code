"""Agent Gateway internal API modules.

Note: `apps/agent-gateway` is not a Python package (hyphenated path), so modules in this
folder are imported by adding the gateway root to `sys.path` in `app/main.py`.
"""

